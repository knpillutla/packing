spring.application.name=picking
# This is the default:
spring.cloud.config.uri=${CONFIG_SERVICE}
management.security.enabled=false
spring.cloud.stream.bindings.pick-out.group=packing-service-group
spring.cloud.stream.bindings.springCloudBusOutput.group=packing-service-group